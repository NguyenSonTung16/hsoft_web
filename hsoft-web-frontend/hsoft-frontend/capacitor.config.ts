import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hsoft.app',
  appName: 'hsoftApp',
  webDir: 'www',
  server: {
    androidScheme: 'http',
  },
};

export default config;
